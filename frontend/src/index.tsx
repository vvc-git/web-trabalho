import React from "react";
import ReactDOM from "react-dom";
import { App } from "./App";

// Renderiza o componente App dentro do elemento com o id "root" do DOM
ReactDOM.render(<App />, document.getElementById("root"));

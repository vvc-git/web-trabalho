import { CSSProperties } from 'react';
import { Theme } from '../../../styles';
export declare const droppableCreateStyles: (theme: Theme, hasKeys: boolean) => {
    placeholder: CSSProperties;
    box: CSSProperties;
};

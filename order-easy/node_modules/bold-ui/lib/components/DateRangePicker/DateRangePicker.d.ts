import { Options as PopperOptions } from '@popperjs/core';
import React from 'react';
import { Theme } from '../../styles';
import { Icons } from '../Icon';
import { ControlledDateRangeCalendarProps } from '../Calendar/RangeCalendar/DateRangeCalendar/ControlledDateRangeCalendar';
import { DateRangePickerInputProps } from './DateRangePickerInput';
export interface DateRangePickerProps extends DateRangePickerInputProps {
    minDate?: Date;
    maxDate?: Date;
    icon?: Icons;
    calendarProps?: Partial<ControlledDateRangeCalendarProps>;
    popperProps?: PopperOptions;
    onFocus?(e: React.FocusEvent<HTMLDivElement>): void;
    onBlur?(e: React.FocusEvent<HTMLDivElement>): void;
    onlyWeeks?: boolean;
}
export interface Week {
    start: Date;
    end: Date;
}
export declare function DateRangePicker(props: DateRangePickerProps): JSX.Element;
export declare namespace DateRangePicker {
    var defaultProps: Partial<DateRangePickerProps>;
}
export declare const disableByWeekRange: (minDate: Date, maxDate: Date) => (week: Week) => boolean;
export declare const createStyles: (theme: Theme) => {
    root: {
        zIndex: number;
        background: string;
        boxShadow: string;
        borderRadius: number;
        padding: string;
        outline: string;
    };
};
